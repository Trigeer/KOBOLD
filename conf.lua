-- require("constants")

function love.conf(t)
    -- t.window.width  = ScreenWidth * Scaling
    -- t.window.height = ScreenHeight * Scaling
    t.window.width  = 612
    t.window.height = 480
    t.console = true
end