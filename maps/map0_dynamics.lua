local map0 = {
    events = {
        {
            flags    = {},
            loopTime = 0,
            code = function (sectors, clock, flags, cache)
        
                -- Magick here
        
            end
        }
    },
    triggers = {
        {
            flags = {},
            code = function (visited, camera, flags, cache)
                
                -- Magick here

            end
        }
    }
}

return map0